# Manager Packaging

This subtree contains imported `SHM` packaging artifacts.

Path:

- `/opt/simplehostman/src/packaging/manager`

Contents include:

- env examples
- systemd units
- RPM spec material

These artifacts are now source-aligned with `/opt/simplehostman/release`, but they still preserve legacy `SHM` product boundaries during the migration.
