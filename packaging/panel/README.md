# Panel Packaging

This subtree contains imported `SHP` packaging artifacts.

Path:

- `/opt/simplehostman/src/packaging/panel`

Contents include:

- env examples
- systemd units
- Apache support templates
- PostgreSQL support templates
- RPM spec material

These artifacts are now source-aligned with `/opt/simplehostman/release`, but they still preserve legacy `SHP` product boundaries during the migration.
