{
  "name": "@simplehost/control-web",
  "version": "2605.02.21",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "test": "pnpm build && node --test dist/**/*.test.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@simplehost/control-shared": "workspace:*",
    "@simplehost/control-config": "workspace:*",
    "@simplehost/control-contracts": "workspace:*",
    "@simplehost/ui": "workspace:*"
  },
  "description": "Transitional web entrypoint inside the unified control-plane app"
}
