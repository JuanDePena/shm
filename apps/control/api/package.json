{
  "name": "@simplehost/control-api",
  "version": "2604.19.08",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@simplehost/control-shared": "workspace:*",
    "@simplehost/panel-config": "workspace:*",
    "@simplehost/panel-contracts": "workspace:*",
    "@simplehost/panel-database": "workspace:*"
  },
  "description": "Transitional API entrypoint inside the unified control-plane app"
}
