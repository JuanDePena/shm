{
  "name": "@simplehost/control-api",
  "version": "2604.28.18",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@simplehost/control-shared": "workspace:*",
    "@simplehost/control-config": "workspace:*",
    "@simplehost/control-contracts": "workspace:*",
    "@simplehost/control-database": "workspace:*"
  },
  "description": "Transitional API entrypoint inside the unified control-plane app"
}
