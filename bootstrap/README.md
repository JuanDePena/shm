# Bootstrap Inventory

This directory contains bootstrap and seed material for the unified source workspace.

Path:

- `/opt/simplehostman/src/bootstrap`

Primary file:

- `/opt/simplehostman/src/bootstrap/apps.bootstrap.yaml`

This tree remains useful for bootstrap, import/export, and disaster recovery, while desired state continues moving into the product database and control-plane workflows.
