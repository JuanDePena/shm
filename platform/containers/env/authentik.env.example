AUTHENTIK_POSTGRESQL__HOST=10.89.0.1
AUTHENTIK_POSTGRESQL__PORT=5432
AUTHENTIK_POSTGRESQL__NAME=app_authentik
AUTHENTIK_POSTGRESQL__USER=app_authentik
AUTHENTIK_POSTGRESQL__PASSWORD=replace-with-root-only-secret
AUTHENTIK_SECRET_KEY=replace-with-root-only-secret
AUTHENTIK_ERROR_REPORTING__ENABLED=false
AUTHENTIK_DISABLE_UPDATE_CHECK=true

# First-start bootstrap values. Keep live values only in the root-owned env file.
# AUTHENTIK_BOOTSTRAP_PASSWORD=replace-with-root-only-first-start-secret
# AUTHENTIK_BOOTSTRAP_EMAIL=admin@example.com
