# Import this key with pdnsutil instead of placing it directly in pdns.conf.
# Example:
#   pdnsutil tsigkey import axfr-internal. hmac-sha256 '__TSIG_SECRET_BASE64__'

TSIG_NAME=axfr-internal.
TSIG_ALGORITHM=hmac-sha256
TSIG_SECRET_BASE64=__TSIG_SECRET_BASE64__
