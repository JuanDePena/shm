# Platform Assets

This directory contains canonical host and container templates for the unified source workspace.

Path:

- `/opt/simplehostman/src/platform`

It includes:

- host hardening material
- Apache templates
- container templates
- database templates
- DNS templates
- WireGuard templates

These assets are imported from the former manager-owned platform tree and now belong to the unified source workspace.
