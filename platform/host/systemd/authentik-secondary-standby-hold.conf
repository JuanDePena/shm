# Install this drop-in only on the passive secondary node.
# It prevents accidental Authentik startup before PostgreSQL apps has been
# promoted and the operator has intentionally released IAM standby.
[Unit]
ConditionPathExists=/etc/simplehost/iam/authentik/SECONDARY_PROMOTED
